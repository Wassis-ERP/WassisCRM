Avatar — foto do usuário ou iniciais sobre o gradiente azul da marca. Usado no header, em cards do Kanban e em listas.

```jsx
<Avatar name="Vinícius Assis" />
<Avatar name="Maria Silva" src="https://…/foto.jpg" size={32} />
```

Sem `src`, deriva as iniciais de `name`. `size` controla o diâmetro.
